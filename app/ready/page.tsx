import { Navigation } from '@/components/navigation'
import { Footer } from '@/components/footer'

export default function ReadyPage() {
  return <div className="min-h-screen bg-background"><Navigation /><main><section className="border-b border-border bg-muted/30"><div className="mx-auto max-w-4xl px-6 py-20 sm:px-8 lg:py-28"><p className="text-sm font-semibold uppercase tracking-[0.14em] text-secondary">Cleared for takeoff</p><h1 className="mt-4 text-5xl font-semibold tracking-[-0.04em] sm:text-6xl">Preflight is <em>installed.</em></h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">Thanks for installing. Open Preflight alongside your next campaign setup and check every link before it spends a dollar.</p></div></section><section className="mx-auto max-w-4xl px-6 py-16 sm:px-8 lg:py-24"><div className="grid gap-12"><article><h2 className="text-2xl font-semibold">Getting started</h2><div className="mt-6 grid gap-5">{['Open the Preflight popup from your toolbar.','Paste the links or creatives you plan to launch.','Read the pass/fail report for each check.','Fix what is flagged, and launch with confidence.'].map((step, i) => <div key={step} className="flex gap-4 rounded-xl bg-muted/40 p-5"><span className="flex size-8 shrink-0 items-center justify-center rounded-full bg-primary text-sm font-semibold text-primary-foreground">{i + 1}</span><p className="pt-1 text-muted-foreground">{step}</p></div>)}</div></article><article><h2 className="text-2xl font-semibold">Stays private</h2><p className="mt-4 leading-8 text-muted-foreground">Preflight checks run in your browser. There is no account, nothing is uploaded, and removing the extension removes everything with it.</p></article></div></section></main><Footer /></div>
}
